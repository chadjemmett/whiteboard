return a string of ascii characters.


const asciiRenderer = (max_x, max_y, pixels) => {
  // pixels is an array of stringified arrays.
    // They look like this "[0, 0, 99]" where the first one is the x coordinate. The second one is the y coordinate. The third is the number of the ascii character.

}
