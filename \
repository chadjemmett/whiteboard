const cardinalDirections = ["east", "west", "north", "south"]
const opposite = {west: "east", east: "west", south: "north", north: "south"}
console.log(opposite["east"])


class UndirectedTree {
  constructor(value) {
    this.value = value
      // we have to get the dir1 and dir2
    // this.directions = {left: dir1, right: dir2, momDir: mommy}
    this.left = null
    this.right = null
    this.parent = null
  }

  insert(value) {
    if(value < this.value) {
      if(this.left === null)
    }
  }

}


const t = new UndirectedTree(5)

console.log(t)
