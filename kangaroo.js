


const kangaroos = (x1, v1, x2, v2) =>  {
  // take the one thats first.
  // take a jump. add it to the array.
  // the second one takes a jump.
  // check that number 
  let firstKangaroo;

  if(x1 > x2) {
    firstKangaroo = x1
    secondKangaroo = x2
  } else {
    firstKangaroo = x2
    secondKangaroo = x1
  } 
  while (firstKangaroo < 10000) {
    firstKangaroo += 



  }





// kangaroo 1. starts at 1 jumps 1 meter a jump
// kangaroo 2 starts at 2. Jumps 2 meters a jump
// When do they land on the same spotl.


kangaroos(0, 3, 4, 2)

